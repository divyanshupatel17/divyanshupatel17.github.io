Source design assets — logos, icons, photos, illustrations used on the site.

Not consumed at build time; final optimized copies used by the app live under `public/` once the
React build exists.

Delete this file once real assets are added.
